# Google Finance Quote Scraper

This actor is implemented in Rust and built with `.actor/Dockerfile`. See `../README.md` for its input, output, retry behavior, focused tests, and local image smoke instructions.
