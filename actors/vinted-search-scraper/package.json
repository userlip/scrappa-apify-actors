{
  "name": "vinted-search-scraper",
  "version": "1.0.0",
  "private": true,
  "description": "Workspace scripts for the Rust Vinted Search Scraper actor.",
  "scripts": {
    "build": "cargo build --release --locked",
    "start": "cargo run --release --locked",
    "start:dev": "cargo run",
    "test": "cargo test --locked",
    "test:dev": "cargo test",
    "typecheck": "cargo check --locked"
  },
  "dependencies": {
    "apify": "^3.2.6"
  },
  "devDependencies": {
    "@types/node": "^20.17.10",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
