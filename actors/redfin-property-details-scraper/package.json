{
  "name": "redfin-property-details-scraper",
  "version": "1.0.0",
  "private": true,
  "description": "Actor-local npm wrappers for the Rust build and test commands.",
  "scripts": {
    "build": "sh scripts/cargo.sh build --locked",
    "test": "sh scripts/cargo.sh test --locked",
    "typecheck": "sh scripts/cargo.sh check --locked"
  }
}
