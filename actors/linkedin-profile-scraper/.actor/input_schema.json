{
  "title": "LinkedIn Profile Scraper ($0.30/1k results)",
  "description": "Scrape public LinkedIn profile data including experience, education, skills, and more.",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "urls": {
      "title": "LinkedIn Profile URLs",
      "type": "array",
      "description": "Recommended. Process many LinkedIn profile URLs in one Apify run so run startup and storage overhead are shared across results.",
      "editor": "stringList",
      "minItems": 1,
      "prefill": [
        "https://www.linkedin.com/in/williamhgates",
        "https://www.linkedin.com/in/satyanadella"
      ],
      "items": {
        "type": "string",
        "minLength": 1,
        "pattern": "^(https?://)?((www|m|[a-z]{2,3})\\.)?linkedin\\.com/in/[a-zA-Z0-9._-]+/?"
      }
    },
    "url": {
      "title": "Legacy Single LinkedIn Profile URL",
      "type": "string",
      "description": "Backward-compatible single profile URL. Prefer URLs for normal usage, especially when processing more than one profile.",
      "editor": "textfield",
      "minLength": 1,
      "prefill": "https://www.linkedin.com/in/williamhgates",
      "pattern": "^(https?://)?((www|m|[a-z]{2,3})\\.)?linkedin\\.com/in/[a-zA-Z0-9._-]+/?"
    },
    "use_cache": {
      "title": "Use Cache",
      "type": "boolean",
      "description": "Use cached data if available to reduce costs and speed up requests",
      "editor": "checkbox",
      "default": true
    },
    "maximum_cache_age": {
      "title": "Maximum Cache Age",
      "type": "integer",
      "description": "Maximum age of cached data in seconds (default: 2592000 = 30 days). Must be at least 1 second. Only used if use_cache is enabled.",
      "editor": "number",
      "minimum": 1,
      "default": 2592000
    }
  }
}
