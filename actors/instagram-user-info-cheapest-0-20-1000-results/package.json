{
  "name": "instagram-user-info-cheapest-0-20-1000-results",
  "version": "0.1.0",
  "private": true,
  "description": "Test adapter for the Rust Instagram User Info actor.",
  "scripts": {
    "test": "if command -v cargo >/dev/null 2>&1; then cargo test --locked; else docker run --rm -v \"$PWD:/work\" -w /work rust:1.90-slim-bookworm cargo test --locked; fi"
  }
}
