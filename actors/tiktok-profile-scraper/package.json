{
  "name": "tiktok-profile-scraper",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "docker run --rm -v \"$PWD:/work\" -w /work rust:1.90-slim-bookworm cargo test --locked"
  }
}
