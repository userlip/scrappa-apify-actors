# TikTok Profile Scraper

This actor is built as a Rust binary from the actor-local `Cargo.toml` and `src/main.rs`.

Run the focused actor tests with `cargo test --locked`. The public input, dataset output, and `OUTPUT` key-value-store record are documented in the actor README.
