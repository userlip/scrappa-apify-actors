{
  "title": "Google Maps Business Details",
  "description": "Extract detailed information for a specific business from Google Maps including ratings, reviews, photos, and operating hours.",
  "type": "object",
  "schemaVersion": 1,
  "anyOf": [
    {
      "required": [
        "business_ids"
      ]
    },
    {
      "required": [
        "business_id"
      ]
    }
  ],
  "properties": {
    "business_ids": {
      "title": "Business IDs",
      "type": "array",
      "description": "Recommended. Process many Google Maps business IDs in one Apify run so run startup and storage overhead are shared across results.",
      "editor": "stringList",
      "minItems": 1,
      "maxItems": 50,
      "prefill": [
        "0x808fba02425dad8f:0x6c296c66619367e0"
      ],
      "items": {
        "type": "string",
        "minLength": 1
      }
    },
    "business_id": {
      "title": "Legacy Single Business ID",
      "type": "string",
      "description": "Backward-compatible single Google Maps business ID in format: 0x[hex]:0x[hex]. Prefer business_ids for normal usage, especially when processing more than one business.",
      "editor": "textfield",
      "prefill": "0x808fba02425dad8f:0x6c296c66619367e0"
    },
    "use_cache": {
      "title": "Use Cache",
      "type": "boolean",
      "description": "Use cached results if available to reduce costs and speed up results",
      "editor": "checkbox",
      "default": true
    },
    "maximum_cache_age": {
      "title": "Maximum Cache Age (seconds)",
      "type": "integer",
      "description": "Maximum age of cached results in seconds. Set to 0 to always fetch fresh data.",
      "editor": "number",
      "minimum": 0,
      "default": 3600
    }
  }
}
