{
  "title": "LinkedIn Company Scraper",
  "description": "Scrape public LinkedIn company pages to extract comprehensive company information, employees, posts, funding, locations, and specialties.",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "urls": {
      "title": "LinkedIn Company URLs",
      "type": "array",
      "description": "Recommended. Process many LinkedIn company URLs in one Apify run so run startup and storage overhead are shared across results.",
      "editor": "stringList",
      "minItems": 1,
      "prefill": [
        "https://www.linkedin.com/company/microsoft",
        "https://www.linkedin.com/company/openai"
      ],
      "items": {
        "type": "string",
        "minLength": 1
      }
    },
    "url": {
      "title": "Legacy Single LinkedIn Company URL",
      "type": "string",
      "description": "Backward-compatible single company URL. Prefer URLs for normal usage, especially when processing more than one company.",
      "editor": "textfield",
      "minLength": 1,
      "prefill": "https://www.linkedin.com/company/microsoft"
    },
    "use_cache": {
      "title": "Use Cache",
      "type": "boolean",
      "description": "Whether to use cached results if available",
      "editor": "checkbox",
      "default": false
    },
    "maximum_cache_age": {
      "title": "Maximum Cache Age",
      "type": "integer",
      "description": "Maximum age of cached results in seconds. Only used when 'Use Cache' is enabled.",
      "editor": "number",
      "minimum": 1
    }
  }
}
