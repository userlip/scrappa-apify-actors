{
  "title": "Google Maps Search",
  "description": "Search for businesses on Google Maps with a simple query.",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "query": {
      "title": "Search Query",
      "type": "string",
      "description": "What to search for (e.g., 'restaurants in NYC', 'coffee shops', 'plumber')",
      "editor": "textfield",
      "prefill": "starbucks times square new york"
    },
    "hl": {
      "title": "Language (HL)",
      "type": "string",
      "description": "Language code or BCP 47 language tag (e.g., 'en', 'de', 'es', 'fr' or with region 'en-US', 'de-DE')",
      "editor": "textfield",
      "default": "en",
      "pattern": "^[a-zA-Z]{2}(-[a-zA-Z]{2})?$",
      "maxLength": 5
    },
    "gl": {
      "title": "Region (GL)",
      "type": "string",
      "description": "ISO 3166-1 alpha-2 country code for region-specific results (e.g., 'us', 'de', 'fr', 'uk')",
      "editor": "textfield",
      "prefill": "us",
      "pattern": "^[a-zA-Z]{2}$",
      "maxLength": 2
    },
    "debug": {
      "title": "Debug Mode",
      "type": "boolean",
      "description": "Enable Scrappa debug output for troubleshooting. This is only useful for accounts with debug access.",
      "editor": "checkbox",
      "default": false
    },
    "use_cache": {
      "title": "Use Cache",
      "type": "boolean",
      "description": "Use cached results if available to reduce costs and speed up results",
      "editor": "checkbox",
      "default": true
    },
    "maximum_cache_age": {
      "title": "Maximum Cache Age (seconds)",
      "type": "integer",
      "description": "Maximum age of cached results in seconds. Set to 0 to always fetch fresh data.",
      "editor": "number",
      "minimum": 0,
      "default": 3600
    },
    "fallback_zoom": {
      "title": "Fallback Zoom",
      "type": "integer",
      "description": "Zoom level used if the simple search endpoint has a transient upstream failure and the actor retries through advanced search.",
      "editor": "number",
      "minimum": 3,
      "maximum": 21,
      "default": 13
    }
  },
  "required": [
    "query"
  ]
}
