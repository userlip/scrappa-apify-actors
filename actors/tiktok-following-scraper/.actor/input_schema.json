{
  "title": "TikTok Following Scraper",
  "description": "Extract public TikTok accounts followed by a user by username, profile URL, or numeric user ID.",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "profile": {
      "title": "TikTok Profile",
      "type": "string",
      "description": "TikTok username with or without @, full HTTPS TikTok profile URL, or numeric user ID up to 30 digits. Bare numeric values are treated as user IDs; prefix numeric usernames with @.",
      "editor": "textfield",
      "prefill": "@tiktok",
      "pattern": "^(?![0-9]{31,}$)(?:@?[A-Za-z0-9._]{2,255}|[0-9]{1,30}|[Hh][Tt][Tt][Pp][Ss]://([^/]+\\.)?[Tt][Ii][Kk][Tt][Oo][Kk]\\.com/@[A-Za-z0-9._]{2,255}/?(?:[?#].*)?)$"
    },
    "count": {
      "title": "Following Per Page",
      "type": "integer",
      "description": "Number of followed accounts to return from this page. Scrappa accepts 1-50.",
      "editor": "number",
      "minimum": 1,
      "maximum": 50,
      "default": 10
    },
    "time": {
      "title": "Pagination Time",
      "type": "integer",
      "description": "Following pagination token/time marker from a previous run. Leave empty for the first page. The actor also accepts cursor as an alias when provided through API input.",
      "editor": "number",
      "minimum": 0
    }
  },
  "required": ["profile"]
}
