# Kununu Jobs Scraper

Apify actor source for `kununu-jobs-scraper`, implemented in Rust. See `.actor/README.md` for marketplace-facing documentation.

Run the focused actor checks from this directory:

```bash
cargo test --locked
docker build -f .actor/Dockerfile -t kununu-jobs-scraper .
```
