{
  "title": "Search YouTube data",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "q": {
      "title": "Query",
      "type": "string",
      "description": "Search query",
      "editor": "textfield",
      "prefill": "Javascript tutorial"
    },
    "sort": {
      "title": "Sort",
      "type": "array",
      "description": "Sort by: relevance, rating, upload_date, view_count",
      "editor": "select",
      "items": {
        "type": "string",
        "enum": ["relevance", "rating", "upload_date", "view_count"]
      },
      "prefill": ["relevance"]
    },
    "duration": {
      "title": "Duration",
      "type": "array",
      "description": "Video duration: short, medium, long",
      "editor": "select",
      "items": {
        "type": "string",
        "enum": ["short", "medium", "long"]
      },
      "prefill": ["short"]
    },
    "upload_date": {
      "title": "Upload date",
      "type": "array",
      "description": "Upload date filter. Mapped to Scrappa publishedAfter.",
      "editor": "select",
      "items": {
        "type": "string",
        "enum": ["hour", "today", "week", "month", "year"]
      },
      "prefill": ["hour"]
    },
    "limit": {
      "title": "Limit",
      "type": "integer",
      "description": "Maximum number of results to return",
      "default": 10,
      "minimum": 1,
      "maximum": 20
    },
    "continuation": {
      "title": "Continuation",
      "type": "string",
      "description": "Pagination token for next page",
      "editor": "textfield"
    },
    "type": {
      "title": "Type",
      "type": "array",
      "description": "Filter by result type",
      "editor": "select",
      "items": {
        "type": "string",
        "enum": ["all", "video", "channel", "playlist", "movie"]
      },
      "prefill": ["all"]
    }
  },
  "required": ["q"]
}
