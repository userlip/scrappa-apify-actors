# Google Images Scraper

Scrappa-powered Apify Actor for Google Images search results. Use `queries` to run multiple keyword searches in one Apify run; legacy single-query `q` still works. See `.actor/README.md` for the Apify Store listing content, input example, and output shape.
