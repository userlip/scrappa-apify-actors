# TrustedShops Shop Profile Scraper

Rust Apify Actor for enriching TrustedShops TSIDs and profile URLs through Scrappa.

Run the actor-local tests with `cargo test --locked`. Apify builds the image from `.actor/Dockerfile`.

See [.actor/README.md](.actor/README.md) for marketplace-facing usage docs.
